{
  "version": 2,
  "builds": [
    {
      "src": "src",
      "use": "@nestjs/cli build"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/dist",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
    }
  ]
}
